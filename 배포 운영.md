# 배포 운영

## 핵심 요약

물살림 AI는 시연용 서버와 공개·파일럿용 서버 구성을 분리한다. 인증키는 서버 환경변수에만 두고, 최종 랜딩은 절수 전환 의사결정 대시보드로 연결한다.

## 기록

### 공개·파일럿 배포 서버 보강  <!-- (2026-07-24) -->

- `server-production.js`는 루트에서 `mulsallim-final.html`을 제공한다.
- IP당 분당 90회 요청 제한, KRC 응답 5분 캐시, 최대 366일 조회 검증, 정적 경로 이탈 차단을 적용했다.
- 전용 임시 포트 테스트에서 루트 200과 잘못된 수위 조회 요청 400을 확인했다.
- 인증키는 `KRC_SERVICE_KEY` 환경변수로만 전달한다.
- 관련: [[물살림AI]], [[KRC API 연동]], [[절수 전환 의사결정]]

### Cloudflare Pages HTTPS 전환  <!-- (2026-07-25) -->

- 상위망이 80/443 인입 차단 → 오리진 직접 HTTPS 불가. Cloudflare Pages(`https://mulsallim-ai.pages.dev`) + Functions 프록시로 고정 HTTPS 확보. GitHub `main` push 시 자동 빌드(`npm run build:pages`, 출력 `dist`).
- 공개 운영 서버는 `server-production-v12.js`(Docker `mulsallim-ai`, `nmlab16.korea.ac.kr:4184`). 루트는 `mulsallim-public-county-v12.html` 제공.
- Function `functions/api/[[path]].js`가 `/api/*`를 오리진으로 서버간 프록시. 오리진은 **호스트명**(`nmlab16.korea.ac.kr`)으로 지정 — 생 IP는 Cloudflare가 `error 1003`으로 거부.
- **블로커 A(해결):** `/api` HTTP 522(비표준 포트 4184 도달불가)는 `cloudflared tunnel --url http://nmlab16.korea.ac.kr:4184`(로컬 PC)로 오리진 앞 HTTPS 터널을 두고 Function `DEFAULT_ORIGIN`을 그 trycloudflare 주소로 바꿔 해결. HTTPS 전 흐름 검증 완료. ⚠️ 임시 터널: 프로세스 유지 필요, 재시작 시 주소 변경. 상세·영구화: [[34_심사표_100점_자가진단_및_배포블로커_20260725]]
- 빌드는 CSP-safe(엄격 CSP, eval 3종 제거→`<script>` 주입), 라이브 v12 세트만 배포(죽은 파일 102개 정리).
- 관련: [[물살림AI]], [[34_심사표_100점_자가진단_및_배포블로커_20260725]]

## 다음 할 일

- [ ] 터널 영구화: 오리진 서버에서 named tunnel + `cloudflared service install`(현재는 로컬 임시 터널)
- [ ] 접근 로그 마스킹·상태 모니터링 적용
