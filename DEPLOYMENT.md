# 배포·실데이터 연동 체크리스트

## 정적 배포

이 프로젝트는 빌드 과정 없이 배포할 수 있다.

- GitHub Pages: 저장소 루트 또는 `docs/`에 파일을 올려 Pages 활성화
- Netlify: `04_krc_water_ai` 폴더를 드래그 앤 드롭
- Vercel: 정적 프로젝트로 배포

배포 후 실제 URL을 기획서의 `서비스 URL` 칸에 기입한다.

## API 키 보안

KRC·기상청 API 인증키를 `dashboard.js`에 넣으면 안 된다. 인증키는 서버 환경변수에 보관하고, 브라우저는 자체 백엔드의 `/api/reservoirs`만 호출해야 한다.

권장 응답 형식:

```json
{
  "asOf": "2026-07-31T09:00:00+09:00",
  "source": "KRC reservoir API + KRC drought map + KMA forecast",
  "reservoirs": [{ "id": "...", "name": "...", "rate": 42, "normal": -8, "days": 17 }]
}
```

## 제출 전 확인

- [ ] 화면의 `시연 데이터 모드`를 실제 데이터 기준시각으로 교체
- [ ] 모든 위험도 문구를 실데이터 검증 결과에 맞게 갱신
- [ ] API 장애 시 마지막 성공 데이터의 기준시각 표시
- [ ] 개인정보·개별 농지 정보는 동의 없이 수집하지 않음
- [ ] 모바일 화면에서 저수지 목록·관개 처방 확인
