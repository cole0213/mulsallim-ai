# 실제 KRC API 연동 명세

## API 선택 근거

`한국농어촌공사_농촌용수 저수지 수위정보 조회`는 저수지 코드와 조회 시작·종료일을 받아 저수지명, 위치, 측정일, 수위(m), 저수율(%)을 반환한다. 이력 데이터는 가뭄 전환점 예측과 수위 급감 탐지의 핵심 입력값이다.

## 자체 백엔드 API

### 상태 확인

`GET /api/health`

### 저수지 수위 이력 조회

`GET /api/reservoir-levels?facCode={저수지코드}&from=YYYYMMDD&to=YYYYMMDD`

브라우저는 KRC API를 직접 호출하지 않는다. `server.js`가 환경변수의 인증키를 이용해 KRC API를 호출하고 필요한 필드만 반환한다.

## 실행

PowerShell에서:

```powershell
$env:KRC_SERVICE_KEY='발급받은_인증키'
node server.js
```

공공데이터포털 활용신청 후 발급되는 인증키를 사용한다. 개발계정은 자동승인이며 일일 트래픽 한도와 이용 약관은 포털의 최신 안내를 확인한다.

## 다음 구현 순서

1. 시범 저수지 5~10개의 시설 코드 목록 확정
2. 365일 이력 수집과 저장
3. 대시보드의 시연 데이터를 `/api/reservoir-levels` 응답으로 교체
4. 롤링 검증으로 MAE/MAPE와 경보 F1 산출
5. 절수 시나리오의 가정·한계를 화면과 기획서에 명시
